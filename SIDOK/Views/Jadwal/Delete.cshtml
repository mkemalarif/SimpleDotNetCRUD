@model SIDOK.Models.JadwalJagaViewModel
@{
    ViewData["Title"] = "Hapus Jadwal";
}


<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Jadwal.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Jadwal.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Dokters.Where(d => d.Id == model.Jadwal.DokterId).FirstOrDefault().Nama)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Dokters.Where(d => d.Id == model.Jadwal.DokterId).FirstOrDefault().Nama)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Polis.Where(d => d.Id == model.Jadwal.PoliId).FirstOrDefault().Nama)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Polis.Where(d => d.Id == model.Jadwal.PoliId).FirstOrDefault().Nama)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Jadwal.Hari)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Jadwal.Hari)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Jadwal.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>